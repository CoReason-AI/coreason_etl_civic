name: 'coreason_etl_civic'
version: '1.0.0'
config-version: 2

profile: 'postgres'

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  coreason_etl_civic:
    +materialized: table
    silver:
      +schema: silver
    gold:
      +schema: gold
